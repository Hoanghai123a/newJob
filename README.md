# JobConnect Frontend

## Structure

- `src/`: app code
- `src/routes/`: pages + API routes
- `src/lib/`: shared client/server helpers
- `vite.config.ts`: deploy adapter selection
- `.env.example`: required env vars

## Build

```bash
npm install
npm run build
npm run start
```

## Dev

```bash
npm run dev
```

Local dev now runs at `http://127.0.0.1:3000` to stay aligned with production routing.
If PM2 production is already using port `3000` on the same machine, stop that process before starting dev.

## Env

Set these before deploy:

- `PB_URL`: server-side upstream PocketBase URL
- `VITE_PB_URL`: optional SSR/build-time PocketBase URL

## Production Flow

Recommended production topology for this app:

```text
chamcongchua.com
  -> Cloudflare Tunnel
  -> http://localhost:3000
  -> JobConnect app
  -> PB_URL
  -> http://127.0.0.1:8290
```

Browser requests should stay same-origin and go through `/api/public/pb`.
PocketBase should stay private on the Linux server when possible.

Example `.env` for CentOS / PM2 / Cloudflare Tunnel:

```bash
PB_URL=http://127.0.0.1:8290
VITE_PB_URL=http://127.0.0.1:8290
PB_ADMIN_EMAIL=your-pocketbase-superuser@example.com
PB_ADMIN_PASSWORD=your-pocketbase-superuser-password
```

Keep `.env` only on the server and never commit it. The PM2 configuration loads this file through
Node's `--env-file` option, so production requires Node.js 20.6 or newer.

## PM2 Deploy

```bash
npm install
npm run build
npm run pm2:start
pm2 save
```

On later deploys:

```bash
git pull
npm run deploy
```

`npm run deploy` rebuilds the app, reloads PM2, and saves the PM2 process list.
When `.env` changes, the same command reloads PM2 with the updated values.

If dependencies changed, run the full deploy instead:

```bash
git pull
npm run deploy:full
```

## Deploy

### Vercel

1. Push repo to GitHub.
2. Import repo in Vercel.
3. Leave framework auto-detect on.
4. Build command: `npm run build`
5. Output is generated by Nitro in `.output/`

### Netlify

1. Push repo to GitHub.
2. Import repo in Netlify.
3. Build command: `npm run build`
4. Publish handled by the TanStack Start Netlify plugin.

## Git push

```bash
git status
git add .
git commit -m "chore: prepare deploy config"
git push origin main
```
